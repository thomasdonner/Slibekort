import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // CLAUDE.md er projektets eget dokument, ikke noget Next selv skal skrive i.
  agentRules: false,
};

export default nextConfig;
